# Railway configuration for CRM MVP
# This project has 3 services: api, web, and postgres (provisioned via Railway dashboard)

[build]
# Root directory - Railway will detect subdirectories for each service

[deploy]
# Health checks
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
